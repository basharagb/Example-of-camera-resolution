# Flutter plugins are registered by generated code. Keep MethodChannel entry points
# and camera metadata classes when release shrinking is enabled by a product flavor.
-keep class io.flutter.plugins.** { *; }
-keepattributes *Annotation*
